# Query Module

Manages chat sessions and messages, then calls AI service for answers.

## Endpoints
- `POST /query/sessions`
- `GET /query/sessions`
- `GET /query/sessions/:id`
- `POST /query/sessions/:id/messages`
- `DELETE /query/sessions/:id`
