# Documents Module

Handles admin PDF upload, document listing, detail retrieval, and signed URL generation.

## Endpoints
- `POST /documents/upload`
- `GET /documents`
- `GET /documents/:id`
- `GET /documents/:id/pdf-url`
